/**
 * Represents a file writer which writes plain text to a file.
 */
public class PlainTextFileWriter
{
   /**
    * Writes plain text to a file.
    *
    * @param filename the name of the file to write the plain text to
    * @param textToWrite the string representing the plain text to write to the file
    */
   public static void writeToFile(String filename, String textToWrite)
   {
      // todo: fill implementation
   }
}
