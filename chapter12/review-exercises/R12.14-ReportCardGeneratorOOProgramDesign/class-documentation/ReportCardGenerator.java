/**
 * The report card generator class contains a program which,
 * given an input and output file via the command line,
 * 1. reads a report card data input file
 * 2. generates a report card from the inputted report card data
 * 3. writes the report card to a plain text file
 */
public class ReportCardGenerator
{
   /**
    * Runs the report card generator program.
    */
   public static void main(String[] args)
   {
      // todo: fill implementation
   }
}
