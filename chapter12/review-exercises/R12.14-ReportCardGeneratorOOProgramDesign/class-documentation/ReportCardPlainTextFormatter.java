/**
 * Represents a report card formatter which formats a report card so that its format is plain text.
 */
public class ReportCardPlainTextFormatter
{
   /**
    * Formats a report card so that it is in plain text format, i.e. a string.
    */
   public static String formatReportCard(ReportCard reportCard)
   {
      // todo: fill implementation
   }
}
