/**
 * Prints outputs to the user.
 */
public class UserOutputPrinter
{
   /**
    * Prints an output line.
    * @param outputLine the output line
    */
   public static void printLine(String outputLine)
   {
      if (outputLine != null)
      {
         System.out.println(outputLine);
      }
   }
}
