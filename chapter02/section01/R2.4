R2.4

Give three examples of objects that belong to the String class. Give an example of an
object that belongs to the PrintStream class. Name two methods that belong to the
String class but not the PrintStream class. Name a method of the PrintStream class that
does not belong to the String class.

three examples of objects that belong to the String class:
1) "Hello, World!"
2) "Stilton"
3) ""

example of an object that belongs to the PrintStream class:
1) System.out

name two methods that belong to the String class but not the PrintStream class:
1) length
2) toUpperCase

name a method of the PrintStrem class that does not belong to the String class:
1) print
